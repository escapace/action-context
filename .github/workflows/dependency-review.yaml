name: dependency-review

on:
  merge_group:
    types:
      - checks_requested

  pull_request:
permissions: {}

jobs:
  dependency-review:
    permissions:
      contents: read
    uses: escapace/workflows/.github/workflows/dependency-review.yaml@trunk
